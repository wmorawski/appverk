export enum GeneratableModule {
  Background = "background",
  Typo = "typo",
}

export type ModuleDimensions = {
  width: number;
  height: number;
};

export type ModulePosition = {
  top: number;
  left: number;
};
